Module Globals

    Public allColors As Color() =
    {Colors.Red, Colors.Green, Colors.Blue, Colors.Yellow,
     Colors.Orange, Colors.Purple, Colors.DeepSkyBlue,
     Colors.MidnightBlue, Colors.Gold, Colors.SlateGray, Colors.SlateBlue,
     Colors.LightCyan}

    'Public allColorsStrings As String() = {"Red", "Green", "Blue", "Yellow",
    ' "Orange", "Purple", "DeepSkyBlue",
    ' "MidnightBlue", "Gold", "SlateGray", "SlateBlue",
    ' "LightCyan"}

    'Function colorToIndex(col As Color) As Integer
    '    If allColors.Contains(col) Then
    '        Return Array.IndexOf(allColors, col)
    '    End If
    '    Return Nothing
    'End Function

    'Function colorToString(col As Color) As String
    '    If allColors.Contains(col) Then
    '        Return allColorsStrings(Array.IndexOf(allColors, col))
    '    End If
    '    Return Nothing
    'End Function

End Module
